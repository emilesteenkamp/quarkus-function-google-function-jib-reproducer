# quarkus-function-google-function-jib-reproducer Project

Project to reproduce jib breaking when adding cloud functions dependency.

To reproduce run:

```shell
quarkus build
```

or

```shell
./gradlew clean build
```
